fastapi==0.110.3
# Pin pymongo to a modern version that supports current TLS behavior
pymongo==4.4.0
# Ensure `click` is new enough for `typer` (resolves Vercel uv lock conflict)
click>=8.2.1
# Required when using `mongodb+srv://` URIs
dnspython>=2.3.0
# Ensure CA bundle availability for TLS
certifi>=2023.11.1
# Required by `from dotenv import load_dotenv` in backend/main.py
python-dotenv>=1.0.1
voyageai
groq
